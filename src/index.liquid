---
layout: layouts/default.liquid
---

<div class="main-sections">
  <section>
    <h2>Bài Viết Mới</h2>
    {% assign posts = collections.posts %}
    {% assign count = 1 %}
    {% include "postlist.liquid" %}
  </section>

  {% assign tagList = collections | postTags %} {% if tagList.length > 0 %}
  <section>
    <h2>Chủ đề</h2>
    {%- for tag in tagList -%}
    <a href="{{ '/tags/{{ tag.name }}' | url }}">{{ tag.name }}({{ tag.count }})</a>{% if
    forloop.last != true %}<span>, </span>{% endif %} {%- endfor -%}
  </section>
  {% endif %}

  <section>
    <h2>Xem thêm các bài viết</h2>
    {% assign posts = collections.posts %}
    {% assign count = 5 %}
    {% assign offset = 1 %}
    {% include "postlist.liquid" %}
    <a class="articles-link" href="{{ '/posts' | url }}">All the articles</a>
  </section>
</div>

<style>
  .main-sections {
    display: flex;
    flex-direction: column;
    gap: var(--space-7);
  }
  .articles-link {
    display: block;
    margin-top: var(--space-4);
  }
</style>
